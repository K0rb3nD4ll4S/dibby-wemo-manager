{
  "name": "wemo-manager",
  "version": "2.0.3",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "packages/mqtt-bridge",
    "packages/node-red-contrib"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/desktop",
    "build": "npm run build --workspace=apps/desktop",
    "install:all": "npm install"
  }
}
